 
Compile :  javac -sourcepath src -cp ojdbc7.jar hw2.java

Run : java -cp ojdbc7.jar:. hw2 [args1] [args2]

Note : I am running oracle 11g in virtual machine and connecting it to eclipse on my mac.
So i will have to open the java file and change the ip address in the connection string.
